<%# 
 Written by : Michael Watkins
Date : 10/9/24
Desc : A login page for users to input their username and password to login and be sent to the shopping website. 
Confirms and searches db to check if user actually exists.

To-DO : 
-If the user doesn't exist I need to prompt my users to go towards the create an account
-Maybe require either or the username and email but idk how that'd work.
-Popups to communicate errors and allow for error handling. 
-Ability to send a user that is loggin into the site from the login page to the main shopping page. 
 %>

<!DOCTYPE html>
<html>
  <head>
    <title>Login here Fwend</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      body {
        font-family: Arial, sans-serif;
        text-align: center;
        margin-top: 50px;
        background-image: url('https://pbs.twimg.com/media/FULCeORaUAApJcX.jpg:large');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        height: 100vh;
      }

      h1 {
        font-size: 36px;
        color: #fff; /* Set text color to stand out from the background */
        text-shadow: 2px 2px 4px #000; /* Add shadow for better readability */
      }

      h2 {
        font-size: 20px;
        color: #e8dcdc;
      }

      p {
        font-size: 18px;
        color: #fff;
        text-shadow: 1px 1px 3px #000;
      }

      .button-container {
        margin-top: 30px;
        background-color: rgba(0, 0, 0, 0.5); /* Transparent background to make form visible */
        padding: 20px;
        display: inline-block;
        border-radius: 10px;
      }

      label {
        color: #fff;
        font-size: 18px;
        text-shadow: 1px 1px 3px #000;
      }

      .button {
        background-color: #4CAF50; /* Green */
        border: none;
        color: white;
        padding: 10px 20px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 10px;
        cursor: pointer;
        border-radius: 5px;
      }

      .button:hover {
        background-color: #45a049;
      }
      .userLogin-button{
        background-color: #ff6208 /*Orange*/
      }


    </style>
    <script>
      // Function to show an alert for errors
      function showError(message) {
        alert(message);
      }
    </script>
  </head>
  <body>
    <h1>Here to Login pal?</h1>
    <p>Well here you go, get loggin' in bud!</p>

    <div class="button-container">
      <h2>Log In</h2>
      <%= form_tag '/login', method: :post do %>
        <label for="identifier">Username or Email:</label><br>
        <%= text_field_tag :identifier %><br><br>
        
        <label for="password">Password:</label><br>
        <%= password_field_tag :password %><br><br>
        
        <%= submit_tag "Login", class: "button"%>
      <% end %>
      <% if flash[:error] %>
        <script>showError("<%= flash[:error] %>");</script>
      <% end %>
      <p>Don't have an account? <a href="/new">Create one here!</a></p>
    </div>
  </body>
</html>
